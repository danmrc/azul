---
title: "Medindo a inércia da inflação brasileira no R com Rolling Window Regression"
author: "Pedro Cavalcante"
date: '2019-05-20'
output:
  html_document:
    self_contained: FALSE
draft: FALSE
katex: TRUE
categories:
- R
- Finanças
- Política Monetária
slug: inercia-inflacao-rolling-window
tags:
- OLS
- Processos Autogressivos
authors: ["pedrocava"]
---

```{R, warning = FALSE, message = FALSE}

library(ggplot2)
library(dplyr)
library(BETS)

ipca = BETSget(433,
               from = "1998-09-01",
               data.frame = TRUE) # IPCA desde outubro de 98

ipca %>%
  ggplot(aes(x = date, y = value)) +
  geom_line(size = 1.2) +
  xlab("Tempo") +
  ylab("Variação Mensal do IPCA (em %)") +
  geom_hline(yintercept = mean(ipca$value)) # linha horizontal na média


```

Se notarmos a inflação no período $t$ como $\pi_t$, modela-la como um processo autoregressivo de ordem $k$ é estimar o seguinte modelo:

$$\pi_t = \phi_0 + \sum_{i=1}^{k} \phi_i \pi_{t-i} + \epsilon_t$$


```{R}

base = ipca



for(i in 4:nrow(ipca)) {
  
base$primeiro_lag[i] = base$value[i-1]
base$segundo_lag[i] = base$value[i-2]
  
}

base = base[3:nrow(base),]


coeficientes = vector()
janela = 36 # número de meses em cada janela
n = nrow(ipca) - janela - 1

for(i in 1:n) {
  
  modelo = lm(value ~ primeiro_lag + segundo_lag,
              data = base[i:i+janela,])
  
  
  
}

```
